---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: cert-manager
  namespace: cert-manager
spec:
  chart:
    spec:
      chart: cert-manager
      reconcileStrategy: ChartVersion
      version: "1.20.0"
      sourceRef:
        kind: HelmRepository
        name: jetstack
        namespace: cert-manager
      interval: 12h
  install:
    crds: CreateReplace
  upgrade:
    crds: CreateReplace
  interval: 1h0m0s
  values:
    crds:
      enabled: true
      keep: true
    replicaCount: 1
