apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: openbao
  namespace: flux-system
spec:
  interval: 1h
  chart:
    spec:
      chart: openbao
      version: ">=0.28.0"
      sourceRef:
        kind: HelmRepository
        name: openbao
        namespace: openbao
