apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: hello-world-data-local
  labels:
    k8up.io/backup: "true"
  namespace: hello-world
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 100Mi
