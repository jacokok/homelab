apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: hello-world-data
  namespace: hello-world
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
  storageClassName: ""
  volumeName: hello-world-pv
