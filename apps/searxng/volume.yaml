apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: valkey-data
  namespace: searxng
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 100Mi
