apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: searxng-tls
  namespace: searxng
spec:
  secretName: searxng-tls
  commonName: searxng.doink.co.za
  dnsNames:
    - searxng.doink.co.za
  issuerRef:
    name: cloudflare-issuer
    kind: ClusterIssuer
